city_parking_info:
    name: Get parking info
    description: Get local parking info
    fields:
        origin:
            name: Origin
            description: Origin eg 51.330436, 3.802043 or person.fred or zone.home or street, city, country
            required: True
            example: zone.home
            selector:
              text:
        api_mode:
            name: Seety API
            description: Use legacy for the existing implementation or official for Seety's external API
            required: False
            example: official
            selector:
              select:
                options:
                  - legacy
                  - official
        api_key:
            name: Official Seety API key
            description: Optional override. When omitted, the configured or built-in Seety API key is used
            required: False
            selector:
              text:
                type: password
        geoapify_api_key:
            name: Geoapify API key
            description: Overrides the configured Geoapify API key for reverse geocoding
            required: False
            selector:
              text:
                type: password
